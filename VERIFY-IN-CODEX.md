# In-app verification prompt

After restarting Codex desktop, open the Kipreh repository and paste:

```text
Inspect the active global and project Codex instructions. List the registered custom agents available to this task by name and description. Then delegate three read-only checks in parallel: ask North to identify the product specification files, Forge to identify the package scripts and release configuration, and Sentinel to identify existing test or beta-readiness documents. Do not modify files. Return each agent's result and confirm whether multi-agent delegation succeeded.
```
